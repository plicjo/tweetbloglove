%h1 Posts
.row.top-20px
  .col-md-5.bottom-20px
    %ol.list-group.list-group-outer
      - @posts.each do |post|
        %li.list-group-item
          = post.title
          .pull-right
            = link_to 'View', post_path(post), class: 'right-10px left-10px', target: '_blank'
            - if policy(post).edit?
              = link_to edit_post_path(post) do
                .left-10px.inline
                  Edit
            = link_to 'Delete', post_path(post), method: :delete, :data => { :confirm => 'Are you sure you want to delete this post?' }, class: ' left-10px' if policy(post).destroy?
    = will_paginate @posts, renderer: BootstrapPagination::Rails