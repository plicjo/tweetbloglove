- if author_signed_in?
  %h1 Posts
  .row.top-20px
    .col-md-5.bottom-20px
      %ol.list-group.list-group-outer
        - @posts.each do |post|
          %li.list-group-item
            / = link_to 'Delete', post_path(post), method: :delete, :data => { :confirm => 'Are you sure you want to delete this post?' }, class: 'delete btn btn-default btn-xs pull-right left-10px'
            / = link_to 'View', post_path(post), class: 'btn btn-default btn-xs pull-right', target: '_blank'
            = post.title
            = link_to edit_post_path(post) do
              .left-10px.inline.pull-right
                Edit
      = will_paginate @posts, renderer: BootstrapPagination::Rails
- else
  %h3
    %em Please sign in.